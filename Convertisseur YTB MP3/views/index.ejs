<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
    <title>Convertisseur YouTube MP4 To MP3</title>
</head>
<body>
    <div class="top-container">
        <form action="/convert-mp3" method="POST" id="form">
            <h1><i class="fa-brands fa-youtube"></i> Convertisseur YouTube MP3</h1>
            <h4>Entrez l'ID de la vidéo</h4>
            <div>
                <input type="text" name="videoID" placeholder="YouTube ID">
                <button id="convert-btn">Convertir</button>
            </div>
        </form>
    </div>

    <div class="bottom-container">
        <% if(typeof success != "undefined" && success) { %>
            <div class="success">
                <p><%= song_title %></p>
                <a href="<%= song_link %>"><button id="download-btn">TÉLÉCHARGER</button></a>

            </div>
        <% } else if(typeof success != "undefined" && !success) { %>
            <div class="errors">
                <p><%= message %></p>
            </div>
        <% } %>
    </div>

    <!-- Message toujours affiché sous le bouton -->
    <div class="credit-message">
        <p><strong>Créé par Ethan, pour For Hub</strong></p>
    </div>

    <!-- Message positionné en bas de la page -->
    <div class="info-message">
        <p>
            <strong>Comment trouver l'ID d'une vidéo YouTube ?</strong><br>
            L'ID d'une vidéo YouTube se trouve dans le lien de la vidéo, après le "=" jusqu'au "&".<br><br>
            <strong>Exemple :</strong><br>
            <span class="example-url">https://www.youtube.com/watch?v=<strong>U2qDlHPg3h0</strong>&ab_channel=AurelienJoubier</span><br>
            Ici, l'ID est : <strong>U2qDlHPg3h0</strong>.
        </p>
    </div>

</body>
</html>
